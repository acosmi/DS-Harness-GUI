/** Product constants shared by the Electron application entry. */

/** Registered secure resource scheme. */
export const DESKTOP_SCHEME = 'app'
/** Exact renderer authority. */
export const DESKTOP_AUTHORITY = 'dsh-gui'
/** Stable/canary product channels. */
export type DesktopChannel = 'stable' | 'canary'
